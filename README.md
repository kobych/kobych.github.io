# Alexander Zinoviev — Android Developer

A compact, framework-free personal site built for GitHub Pages.

Open `index.html` locally or publish the repository directly with GitHub Pages. All asset paths are relative, so the site also works from a repository subpath.
